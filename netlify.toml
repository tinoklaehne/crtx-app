[build]
  command = "npm install && npm run build"
  publish = "out/"

[build.environment]
  # Set AIRTABLE_API_KEY and AIRTABLE_BASE_ID (or NEXT_PUBLIC_*) in Netlify dashboard → Site settings → Environment variables. Do not commit keys here.
  NODE_OPTIONS = "--max-old-space-size=8192"